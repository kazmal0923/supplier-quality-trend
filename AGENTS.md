# 開発エージェント運用ルール

## 1. 役割

このリポジトリでは、Cursorは実装担当です。
業務仕様・計算定義・対象範囲を独断で変更してはいけません。

## 2. 作業開始時

必ず次の順序で確認してください。

1. `README.md`
2. `docs/00_project-overview.md`
3. `docs/05_current-status.md`
4. 対象機能の `docs/features/FEATURE-xxx_*.md`
5. 関連する `docs/decisions/ADR-xxx_*.md`
6. `docs/04_acceptance-criteria.md`
7. 既存コード、設定、テスト

## 3. 実装前

- 最初に既存コードと仕様への影響範囲を調査してください。
- コード変更前に実装計画を提示してください。
- 仕様の矛盾、不足、実現上の問題を発見した場合は報告してください。
- 不明な業務仕様を推測で確定しないでください。
- 変更範囲は必要最小限にしてください。

## 4. 実装時

- 現在の仕様と受入条件に従ってください。
- 既存の命名規則、設計方針、ディレクトリ構成を尊重してください。
- 認証情報、個人情報、機密データをコードやGitへ含めないでください。
- `.env` などの機密設定はGit管理対象外にしてください。
- 仕様変更が必要な場合は、実装を進める前に影響範囲を明示してください。

## 5. 作業完了時

必ず次を実施してください。

1. 変更内容を確認する
2. テストを実行する
3. ビルドまたは型チェックを実行する
4. 受入条件との一致を確認する
5. `docs/05_current-status.md` を更新する
6. 必要に応じて `docs/decisions/` を更新する
7. `docs/testing/` にテスト結果を記録する
8. `docs/changes/change-log.md` を更新する
9. Git差分を確認する
10. 成功条件を満たした場合のみCommit・Pushする
11. コミットハッシュ、Push先ブランチ、実行結果を報告する

## 6. Pushゲート

以下をすべて満たす場合のみPushしてください。

- 実装が完了している
- テストが成功している
- ビルドまたは型チェックが成功している
- 受入条件を満たしている
- docsが現在の実装状態と一致している
- 仕様変更・設計判断が記録されている
- 機密情報が含まれていない
- Git差分を確認済みである

いずれかを満たさない場合はPushせず、ローカルで修正を続けてください。

## 7. Gitルール

- 原則としてfeatureブランチで作業してください。
- mainブランチへ無断で直接Pushしないでください。
- force pushを行わないでください。
- 1機能または1修正を、意味のあるコミット単位にしてください。
- コミットメッセージは変更内容が分かる形式にしてください。
- コードだけを更新し、関連docsを古いまま残してはいけません。

## 8. 状態の定義

- ローカル環境：作業中の記憶
- featureブランチ：実装済み・確認待ちの共有記憶
- mainブランチ：正式に確定した共有記憶

Pushは、検証済みのコード・仕様・判断・進捗を共有記憶として確定する操作です。
