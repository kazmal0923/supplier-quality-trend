# 開発エージェント運用ルール

## 1. 役割

このリポジトリでは、Cursorは実装担当です。

業務仕様、計算定義、対象範囲、データの意味を独断で変更してはいけません。

不明点や未確定事項がある場合は、推測で実装せず、人間へ確認してください。

---

## 2. 作業開始時

作業開始時は、最初にGitの状態を確認してください。

```powershell
git status
git branch --show-current
```

未コミットの変更がないことを確認してから、GitHub上の最新状態を取得してください。

```powershell
git pull --ff-only
```

未コミットの変更がある場合は、内容を確認せずに`git pull`してはいけません。

その後、原則として次の順序で文書を確認してください。

1. `README.md`
2. `AGENTS.md`
3. `docs/00_project-overview.md`
4. `docs/01_current-state-analysis.md`
5. `docs/02_requirements.md`
6. `docs/03_data-specification.md`
7. `docs/04_acceptance-criteria.md`
8. `docs/05_current-status.md`
9. `docs/06_development-setup.md`
10. `docs/07_architecture.md`
11. `docs/08_security-and-data-handling.md`
12. 対象機能の`docs/features/FEATURE-xxx_*.md`
13. 関連する`docs/decisions/ADR-xxx_*.md`
14. 関連する`docs/changes/`
15. 既存コード、設定、テスト

すべての文書を機械的に読むのではなく、対象機能と影響範囲に関連する内容を重点的に確認してください。

---

## 3. 実装前

- 最初に既存コードと仕様への影響範囲を調査する
- コード変更前に実装計画を提示する
- この段階では、明示的な承認があるまでコードや文書を変更しない
- 仕様の矛盾、不足、実現上の問題を発見した場合は報告する
- 不明な業務仕様を推測で確定しない
- 変更範囲は必要最小限にする
- 既存機能への影響を確認する
- 必要なテスト方法を事前に明確にする
- 受入条件との対応関係を確認する

実装計画には、少なくとも以下を含めてください。

1. 変更対象
2. 変更理由
3. 実装方針
4. 影響範囲
5. テスト方法
6. 更新するドキュメント
7. 未確定事項

人間が計画を確認するまで、実装や文書編集を開始しないでください。

---

## 4. 実装時

- 現在の仕様と受入条件に従う
- 既存の命名規則、設計方針、ディレクトリ構成を尊重する
- 画面表示と業務ロジックを分離する
- 業務ロジックとデータアクセス処理を分離する
- 計算式をUIへ直接記述しない
- 同じ業務ルールを複数箇所へ重複実装しない
- 認証情報、個人情報、機密データをコードやGitへ含めない
- `.env`などの機密設定はGit管理対象外にする
- PC固有の絶対パスをソースコードやGit管理対象ファイルへ直接記述しない
- 仕様変更が必要な場合は、実装を進める前に影響範囲を明示する
- 不要なリファクタリングや、対象外の変更を同時に行わない

---

## 5. 作業完了時

必ず次を実施してください。

1. 変更内容を確認する
2. 対象プロジェクトに定義されたテストを実行する
3. 型チェック、Lint、ビルドを実行する
4. 受入条件との一致を確認する
5. `docs/05_current-status.md`を更新する
6. 必要に応じて`docs/decisions/`を更新する
7. `docs/testing/`にテスト結果を記録する
8. `docs/changes/change-log.md`を更新する
9. 必要に応じて`CHANGELOG.md`を更新する（存在する場合のみ）
10. Git差分を確認する
11. 機密情報が含まれていないことを確認する
12. 成功条件を満たした場合のみCommit・Pushする
13. コミットハッシュ、Push先ブランチ、実行結果を報告する

確認コマンド：

```powershell
git status
git diff
git diff --check
```

ステージング後は、次も確認してください。

```powershell
git diff --cached
```

---

## 6. テスト・ビルドが存在しない場合

対象プロジェクトにテスト、型チェック、Lint、ビルドのコマンドが存在しない場合は、無理に実行してはいけません。

その場合は、該当項目を「該当なし」とし、理由を記録してください。

実行可能なアプリコードが存在しない場合は、以下を確認してください。

- Markdownの表示
- Markdown構文
- リンク切れ
- ファイル構成
- 文書間の矛盾
- `git diff --check`
- Git差分
- 機密情報の混入
- `.env.example`に実値が含まれていないこと

この場合、テスト、型チェック、Lint、ビルドは「該当なし」とします。

---

## 7. Pushゲート

以下をすべて満たす場合のみPushしてください。

- 実装または文書変更が完了している
- 必要なテストが成功している
- 必要なビルド、型チェック、Lintが成功している
- 該当しない確認項目について理由が記録されている
- 受入条件を満たしている
- docsが現在の実装状態と一致している
- 仕様変更や設計判断が記録されている
- 機密情報が含まれていない
- Git差分を確認済みである
- Push先ブランチが正しい
- 人間の確認が必要な事項が未解決のまま残っていない

いずれかを満たさない場合はPushせず、ローカルで修正または確認を続けてください。

---

## 8. Gitルール

- 原則としてfeatureブランチで作業する
- mainブランチへ無断で直接Pushしない
- force pushを行わない
- 1機能または1修正を、意味のあるコミット単位にする
- コミットメッセージは変更内容が分かる形式にする
- コードだけを更新し、関連docsを古いまま残さない
- 同じブランチを複数PCで同時に編集しない
- 別PCで作業を継続する場合は、現在のPCでCommit・Pushを完了する
- Pull Requestによる確認後にmainへ統合する
- 正式版として確定した時点でGitタグを作成する

---

## 9. 状態の定義

- ローカル環境：作業中の記憶
- featureブランチ：実装済み・確認待ちの共有記憶
- mainブランチ：正式に確定した共有記憶
- Gitタグ：正式版を固定した記録

Pushは、検証済みのコード、仕様、判断、進捗を共有記憶として確定する操作です。

---

## 10. 複数PCでの開発ルール

本プロジェクトは、複数のPCで作業する可能性があります。

PCごとにローカルの保存先は異なりますが、GitHub上のリポジトリ構成は同一に保ってください。

ローカルのクローン先は、次のプレースホルダー形式で扱います。実際のユーザー名や実パスはGit管理対象ファイルへ記載しません。

```text
C:\Users\<ユーザー名>\Desktop\Projects\quality-defect-rate-dashboard
```

### パスの扱い

ソースコード、設定ファイル、テスト、設計文書では、特定PCの絶対パスを前提にしてはいけません。

原則として、リポジトリ直下を基準にした相対パスを使用してください。

良い例：

- `docs/02_requirements.md`
- `data/samples/sample.csv`
- `src/app/`
- `./config/settings.json`

避ける例：

- 実際のユーザー名を含む絶対パス
- 会社PC・個人PC固有の実パス

絶対パスが必要な場合は、環境変数またはGit管理対象外のローカル設定ファイルで管理してください。

---

## 11. ドキュメント更新ルール

以下を変更した場合は、対応する文書も更新してください。

| 変更内容 | 更新対象 |
|---|---|
| プロジェクトの目的・対象範囲 | `docs/00_project-overview.md` |
| 現行業務・既存Excel・CSV | `docs/01_current-state-analysis.md` |
| 機能・業務要件 | `docs/02_requirements.md` |
| カラム・データ型・計算式 | `docs/03_data-specification.md` |
| 完了条件 | `docs/04_acceptance-criteria.md` |
| 現在の進捗・次の作業 | `docs/05_current-status.md` |
| セットアップ・実行コマンド | `docs/06_development-setup.md` |
| 採用技術・構成・データフロー | `docs/07_architecture.md` |
| 認証情報・データ取扱い | `docs/08_security-and-data-handling.md` |
| 機能仕様 | `docs/features/` |
| 重要な設計判断 | `docs/decisions/` |
| テスト結果 | `docs/testing/` |
| 変更内容 | `docs/changes/change-log.md` |
| バージョン単位の変更 | `CHANGELOG.md`（存在する場合） |

---

## 12. セキュリティ・データ取扱い

詳細は`docs/08_security-and-data-handling.md`に従ってください。

以下をGitHubへCommit・Pushしてはいけません。

- `.env`
- APIキー
- パスワード
- アクセストークン
- Cookie
- 秘密鍵
- 認証済みセッション情報
- 実在する個人情報
- 顧客情報
- 本番データの無加工コピー
- 外部共有が許可されていない社内データ
- 現行Excel原本や実CSV
- 不要なログや一時ファイル

`.env.example`には、環境変数名と説明だけを記載し、実際の値を記載してはいけません。

---

## 13. 作業終了時の報告

作業終了時は、以下を報告してください。

- 変更したファイル
- 実施した内容
- テスト結果
- 型チェック、Lint、ビルド結果
- 該当なしとした項目と理由
- 更新したドキュメント
- 未解決事項
- コミットハッシュ
- Push先ブランチ
- Pull Requestの要否
